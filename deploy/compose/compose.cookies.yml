version: "3.9"
services:
  cookies-extractor:
    build:
      context: ../..
      dockerfile: deploy/docker/cookies-extractor/Dockerfile
    volumes:
      - ../local/cookies:/cookies_extractor/cookies:rw
