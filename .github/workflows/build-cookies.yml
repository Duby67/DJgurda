name: Build Cookies

on:
  push:
    branches: ["cookies"]
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: 'echo "TODO: implement cookies extractor build/deploy"'
